import { HTTP } from '@ionic-native/http/ngx';
export declare class WatchJsonService {
    httpClient: HTTP;
    constructor(httpClient: HTTP);
    getJSONWatchPage(youtubeId: any, options: any, httpClient: any): Promise<any>;
}
